#include <stdio.h>
//#include <string.h>
//#include <math.h>

long long int A[10000007];

int main() {
	//defvals
	long long int i, j, k, T, N;

	scanf("%lld", &N);

	for(i = 0; i < N; i++) {
		scanf("%lld", &A[i])	
	}

		

	/*
	default constructs
	
	long long int i, j, k, T, N;

	//Input into N
	scanf("%lld", &N);

	
	//Loop from 0 -- > N-1
	for(i = 0; i < N; i++) {
		
	}
	
	Print statement
	printf("%lld\n", N);
	
	*/
	return 0;
}
