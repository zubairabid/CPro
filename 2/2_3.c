#include <stdio.h>
//#include <stdlib.h>
//#include <math.h>

int main() {
    int N, Q, i, j, k, l;

    //update with algorithm later
    scanf("%d %d", &N, &Q);
    int a[N];

    for(l = 0; l < N; l++) {
        scanf("%d", &a[i]);
    }

    for(l = 0; l < Q; l++) {
        scanf("%d %d %d", &i, &j, &k);
    }

    for(l = 0; l < N; l++) {
        printf("%d ", a[l1]);
    }
    printf("\n");

    return 0;
}

